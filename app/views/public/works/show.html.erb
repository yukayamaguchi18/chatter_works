<div class="container p-3">
  <div class="row">
    <div class="col-12" id="work-show">
      <%= render 'public/works/show', work: @work, tags: @tags %>
    </div>
  </div>
</div>

<!--chatter modal-->
<%= render 'public/chatters/modal', chatter: @chatter %>

<!--work modal-->
<%= render 'public/works/modal', work: @work %>

<!--tag modal-->
<%= render 'public/work_tags/modal', work: @work %>

<!--comment modal-->
<%= render 'public/comments/modal', work: @work %>
