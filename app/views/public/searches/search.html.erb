<div class="container p-3">
  <% if @model == "Keyword" %>
    <%= render 'public/searches/keyword-result', users: @users, chatters: @chatters, works: @works %>
  <% elsif @model == "WorkTag" %>
    <%= render 'public/searches/tag-result', works: @works %>
  <% else %>
    <p>No results found. Please search again.</p>
  <% end %>
</div>